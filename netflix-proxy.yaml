bind:
  image: ab77/bind
  container_name: bind
  ports:
    - 53:53/udp
  volumes:
    - /opt/netflix-proxy/data:/data
sniproxy:
  image: ab77/sniproxy
  container_name: sniproxy
  net: host
  volumes:
    - /opt/netflix-proxy/data:/data
